name: "{{squad-name}}"
version: 0.1.0
description: "{{description}}"
author: "{{author}}"
license: MIT

triviaiox:
  minVersion: "2.1.0"
  type: squad

components:
  agents:
    - agents/*.yaml
  tasks:
    - tasks/*.yaml
  workflows:
    - workflows/*.yaml
  templates:
    - templates/*.md

dependencies: []

keywords:
  - triviaiox
  - squad
